function GoogleAuth({ divRef }) {
  return (
      <div id="signInDiv" class="font-monospace">
        <center>
          <div ref={divRef} />
        </center>
      </div>
  );
}

export default GoogleAuth;
